# Combined Simplification Plan

**Date:** 2026-04-25 · **Branch:** `refactor`
**Sources:** `frameworks-simplification.md`, `web-frameworks-redesign.md`, `notebooks/baml_spike.livemd`

---

## Core Insight

Both simplification docs describe the same architectural problem from opposite ends:

```
Frameworks (producer) ──> Rho.Comms (transport) ──> LiveView (consumer)
```

The frameworks doc asks how to stop the producer from coupling to the transport.
The web doc asks how to stop the consumer from parsing transport artifacts.
They're one migration with a shared dependency.

The BAML spike adds a third dimension: structured output parsing can be unified
under a single Rust-based engine (`baml_elixir`) that works with any model,
replacing both `ReqLLM.generate_object` (frameworks) and `StructuredOutput` +
`SchemaCoerce` (TypedStructured).

---

## BAML Spike Results (2026-04-25)

All 8 experiments passed. Key findings:

| Finding | Detail |
|---------|--------|
| Static schemas work | BAML + DeepSeek via OpenRouter, ~$0.0001/call |
| Dynamic schema generation | Generate `.baml` files at runtime, BAML re-reads per call |
| TypeBuilder limited | Atom types parse but can't make fields optional; use file generation |
| Discriminant pattern works | `tool` field as tagged union discriminant, reliable across prompts |
| Streaming partials work | Progressive field arrival; `tool` field comes early enough for UI |
| Schema regeneration works | Rewrite `.baml` between calls, new schema picked up immediately |
| Collector works | Token usage, cost, full request/response logging |

**Architecture decisions confirmed:**
- `RhoFrameworks.LLM` → Zoi-defined schemas compiled to `.baml` via `baml_elixir`
- `TypedStructured` → dynamically generated `.baml` files via `baml_elixir`
- `StructuredOutput` + `SchemaCoerce` → dead code after migration

---

## Schema Strategy: Zoi as Source of Truth

### Why Zoi

Zoi is a schema-as-data library — no macros,
just composable functions. A single Zoi schema definition produces:

1. An Elixir struct with enforced keys
2. Dialyzer typespecs via `Zoi.type_spec/1`
3. Validation via `Zoi.parse/2` for LLM response verification
4. A BAML class definition via `RhoBaml.SchemaCompiler.to_baml_class/1`

This avoids maintaining parallel definitions in `.baml` and Elixir.

### What an LLM Function Module Looks Like

```elixir
defmodule RhoFrameworks.LLM.RankRoles do
  use RhoBaml.Function,
    client: "OpenRouter",
    params: [query: :string, role_list: :string, limit: :int]

  @schema Zoi.struct(__MODULE__, %{
    indices: Zoi.array(Zoi.integer()) |> Zoi.describe("1-based indices of most similar roles"),
    reasoning: Zoi.string() |> Zoi.describe("Brief explanation")
  })

  @enforce_keys Zoi.Struct.enforce_keys(@schema)
  defstruct Zoi.Struct.struct_fields(@schema)
  @type t :: unquote(Zoi.type_spec(@schema))

  @prompt ~S"""
  You are a role matching assistant. Given a query and a numbered list of
  existing role profiles, return the indices of the most similar roles.

  Return at most {{limit}} indices.
  Only return numbers that appear in the list.

  Query: {{query}}
  Roles: {{role_list}}

  {{ ctx.output_format }}
  """

  # Generated by `use RhoBaml.Function`:
  # def call(args, opts \\ [])    — sync call, returns {:ok, %__MODULE__{}} | {:error, term()}
  # def stream(args, cb, opts)    — streaming call
end
```

### `use RhoBaml.Function`

Not a macro-heavy system. A thin `__before_compile__` hook that:

1. Reads `@schema` and `@prompt` module attributes
2. Converts Zoi schema → BAML class string via `RhoBaml.SchemaCompiler`
3. Writes the `.baml` file (class + function) to the app's `priv/baml_src/`
4. Defines `call/2` and `stream/3` that:
   - Delegate to `BamlElixir.Client.call/3` or `.sync_stream/4`
   - Validate result with `Zoi.parse(@schema, result)`
   - Return the struct

### `RhoBaml.SchemaCompiler`

Converts Zoi schemas to BAML class definitions (~30 lines):

| Zoi | BAML |
|-----|------|
| `Zoi.string()` | `string` |
| `Zoi.integer()` | `int` |
| `Zoi.float()` | `float` |
| `Zoi.boolean()` | `bool` |
| `Zoi.array(Zoi.integer())` | `int[]` |
| `Zoi.optional()` | `type?` |
| `Zoi.describe("...")` | `@description("...")` |
| `Zoi.map(%{...})` | nested `class { ... }` |

### Where Zoi Applies vs Where It Doesn't

| Use case | Zoi as source of truth? | Why |
|----------|------------------------|-----|
| Frameworks static schemas | **Yes** | Schema defined once, generates struct + `.baml` |
| Future LLM functions | **Yes** | Same pattern scales — add a module, get struct + BAML |
| TypedStructured dynamic | **No** | Tool_defs come from NimbleOptions at runtime |
| BAML client configs | **No** | Infrastructure, hand-written `.baml` files |

---

## App Ownership: Each App Owns Its Schemas

```
apps/rho_baml/                           # Pure library — no priv/, no configs
├── lib/
│   ├── rho_baml.ex                      # helpers (baml_path/1, etc.)
│   ├── rho_baml/function.ex             # `use RhoBaml.Function` hook
│   ├── rho_baml/schema_compiler.ex      # Zoi → BAML class conversion
│   └── rho_baml/schema_writer.ex        # tool_defs → dynamic .baml (added in Phase 4)
└── mix.exs                              # deps: baml_elixir, zoi

apps/rho_frameworks/                     # Owns its schemas AND client configs
├── lib/rho_frameworks/llm/
│   ├── rank_roles.ex                    # Zoi schema + @prompt + use RhoBaml.Function
│   ├── score_lens.ex                    # same pattern
│   └── semantic_duplicates.ex           # same pattern
└── priv/baml_src/                       # Everything BAML needs in one tree
    ├── clients/
    │   ├── openrouter.baml              # cheap DeepSeek
    │   └── anthropic.baml               # Claude for quality tasks
    └── functions/                       # Generated at compile time by RhoBaml.Function
        ├── rank_roles.baml
        ├── score_lens.baml
        └── find_semantic_duplicates.baml

apps/rho/                                # Owns its dynamic schemas
└── priv/baml_src/                       # TypedStructured's BAML tree
    ├── clients/                         # Own copy of client configs
    │   ├── openrouter.baml
    │   └── anthropic.baml
    └── dynamic/                         # Generated at runtime by SchemaWriter
        └── action_<hash>.baml
```

`rho_baml` is a pure library — no `priv/`, no configs. It provides:
- `RhoBaml.Function` behaviour (compile-time Zoi → BAML for static schemas)
- `RhoBaml.SchemaCompiler` (Zoi → BAML class string conversion)
- `RhoBaml.SchemaWriter` (runtime tool_defs → BAML for TypedStructured, added in Phase 4)

Each consumer app owns its full BAML tree: client configs, schemas, and
generated functions. Adding a new LLM function to frameworks means adding
a module to `rho_frameworks/lib/`, not touching `rho_baml`. Client configs
are duplicated per consumer app (they're ~5 lines each).

---

## What Becomes Dead Code

| Module | Lines | Replaced by |
|--------|-------|------------|
| `Rho.StructuredOutput` | 537 | BAML Rust parser |
| `Rho.SchemaCoerce` | 374 | BAML Rust type coercion |
| `Rho.ActionSchema.render_prompt/1` | ~25 | BAML `{{ctx.output_format}}` |

`ActionSchema.build/1` and `ActionSchema.dispatch/3` remain — they handle
the discriminant matching and intent classification that BAML doesn't do.
But `parse_and_dispatch/3` changes from:

```
StructuredOutput.parse(text) → dispatch(parsed, schema, tool_map)
```

to:

```
BAML returns parsed map → dispatch(parsed, schema, tool_map)
```

---

## Combined Phase Plan

**Progress key:** `[ ]` not started · `[~]` in progress · `[x]` done

### [~] Phase 0: `rho_baml` app (1d)

Create the shared BAML infrastructure (compile-time path only).

1. Create `apps/rho_baml/` umbrella app
   - Deps: `baml_elixir ~> 1.0.0-pre.27`, `zoi`
2. Create `RhoBaml.SchemaCompiler` — Zoi schema → BAML class string
3. Create `RhoBaml.Function` — `use` hook that reads `@schema` + `@prompt`,
   generates `.baml` files at compile time, defines `call/2` and `stream/3`
4. Write shared client configs in `priv/baml_src/clients/`
5. Verify with Livebook

**Notebook:** `notebooks/phase0_rho_baml.livemd` — demonstrate Zoi schema
definition, SchemaCompiler output, and a round-trip `RhoBaml.Function.call/2`
against a live model. Show rationale: single schema definition produces
struct + typespec + BAML class. Compare before/after with `ReqLLM.generate_object`.

### [x] Phase 1: LiveEvents + canonical events (1d)

Create the new event transport. Nothing consumes it yet.

1. Create `RhoWeb.LiveEvents` — `subscribe/1`, `broadcast/2`
2. Single PubSub topic per session: `"rho_lv:session:<sid>"`
3. Define canonical event shape: `kind` atoms instead of topic strings
4. Create `normalize_event/3` — Runner emit events → canonical events

**Notebook:** `notebooks/phase1_live_events.livemd` — subscribe to a session's
LiveEvents, trigger an agent run, show canonical events arriving as atoms.
Compare side-by-side with the old Comms string-based topic events.

### [x] Phase 2: Dual-path streaming (1d)

Wire LiveEvents as a second delivery path alongside Comms.

1. Modify `Worker.emit` to also broadcast via LiveEvents
2. Add `handle_info({:live_event, event}, socket)` in SessionLive
3. New atom-based `SessionState.reduce/2` clauses
4. Keep old string-based clauses as fallback
5. Test both paths produce identical state

**Notebook:** `notebooks/phase2_dual_path.livemd` — run an agent session,
collect events from both Comms and LiveEvents in parallel, diff the two
streams to verify identical state transitions.

### [~] Phase 3: Frameworks LLM extraction via BAML + Zoi (1d)

Replace `ReqLLM.generate_object` calls with Zoi-defined BAML functions.

1. Add `rho_baml` dependency to `rho_frameworks`
2. Create `RhoFrameworks.LLM.RankRoles` — Zoi schema + `use RhoBaml.Function`
   - Replaces `rank_similar_via_llm` in `roles.ex`
3. Create `RhoFrameworks.LLM.ScoreLens` — same pattern
   - Replaces `score_via_llm` in `lenses.ex`
4. Create `RhoFrameworks.LLM.SemanticDuplicates` — same pattern
   - Replaces LiteWorker-based dedup in `library.ex`
5. Delete all `Rho.Config.agent_config()` calls from frameworks
6. Per-call model flexibility via `llm_client` option

**Notebook:** `notebooks/phase3_frameworks_llm.livemd` — call each new
`RhoBaml.Function` module (RankRoles, ScoreLens, SemanticDuplicates) with
sample data. Show the Zoi schema, generated `.baml` file, and parsed struct
result. Demonstrate model flexibility by switching `llm_client` between calls.

### [x] Phase 4: TypedStructured via BAML (1.5d)

Replace the custom parser pipeline with BAML.

1. Create `RhoBaml.SchemaWriter` — runtime tool_defs → `.baml` file generation
   (deferred from Phase 0; designed against TypedStructured's actual tool_defs shape)
2. Add `rho_baml` dependency to `rho`
3. Modify `TypedStructured.run/2`:
   - `RhoBaml.SchemaWriter.write!` generates `.baml` from tool_defs
   - `BamlElixir.Client.sync_stream` replaces `ReqLLM.stream_text`
   - Streaming callback → emit `structured_partial` events
4. Add `ActionSchema.dispatch_parsed/3`:
   - Accepts pre-parsed map from BAML, bypasses `StructuredOutput.parse`
   - `parse_and_dispatch/3` retained as fallback
5. Keep `ActionSchema.build/1` and `dispatch/3` — still needed for intent classification
6. `StructuredOutput` is now dead code for TypedStructured (retained for now, can be deleted in cleanup)

**Notebook:** `notebooks/phase4_typed_structured.livemd` — take a set of
real tool_defs, show the generated `.baml` file from SchemaWriter, run a
TypedStructured turn with BAML parsing, and compare output with the old
StructuredOutput + SchemaCoerce pipeline. Demonstrate streaming partials.

### [ ] Phase 5: Scope + AgentJobs + LiteWorker removal (2d)

Replace Runtime and LiteWorker with `RunSpec` + `Runner.run`.

1. Create `RhoFrameworks.Scope` — 3 fields: `organization_id`, `session_id`, `user_id`
2. Add `Scope.from_context/1` bridge
3. Replace `Runtime.t()` → `Scope.t()` throughout frameworks
4. Create `RhoFrameworks.AgentJobs` — thin wrapper: `Task.async` + `Runner.run`
   with minimal `RunSpec` (`tape_name: nil`) + composed emit (heartbeat to parent)
5. Move `proficiency_writer` config from `.rho.exs` to `proficiency.ex` as an
   inline `RunSpec.build(...)` call — it's an internal worker, not a user-facing agent
6. Rewrite skeleton generator + proficiency fan-out to use `AgentJobs`
7. Wire emit callbacks → `LiveEvents.broadcast`
8. Migrate `Rho.Stdlib.Plugins.MultiAgent` from `LiteWorker` to `Runner.run` + Task
9. Delete `build_lite_context`, `resolve_tools` helpers
10. Delete `RhoFrameworks.Runtime`
11. Delete `Rho.Agent.LiteWorker` (565 lines)

**Notebook:** `notebooks/phase5_agent_jobs.livemd` — show AgentJobs spawning
a batch of parallel workers via `Runner.run` + Task, with heartbeat emit
composition. Compare weight: Runner.run with `tape_name: nil` vs old
LiteWorker (demonstrate equivalent behavior, no tape/transformer overhead).
Show Scope struct replacing Runtime.

### [x] Phase 6: Domain events → LiveEvents (0.5d)

Added dual-path LiveEvents broadcasts alongside existing Comms.publish
calls in rho_stdlib and rho_frameworks. Uses the same
`Application.get_env(:rho, :event_broadcaster)` pattern as Worker.

**Covered:**
- MultiAgent: task_requested (3 sites), task_completed, lite event
  streaming, agent_spawned, message_sent, broadcast
- AgentJobs: task_completed, event streaming
- SkeletonGenerator: task_requested
- Proficiency: task_requested
- LensTools: lens_dashboard_init
- Lenses: lens_score_update

**Intentionally Comms-only:** DataTable.Server (internal invalidation),
LiveRender (custom streaming protocol), Hiring demos.

**Notebook:** `notebooks/phase6_domain_events.livemd`

### Pre-Phase 7: Audit simulation module

**Result: No breakage found.** Audited 2026-04-25.

Files checked:
- `demos/hiring/simulation.ex` — coordinator GenServer
- `demos/hiring/tools.ex` — submit_scores tool builder
- `demos/hiring/candidates.ex` — static data, no runtime deps

#### Deleted-module references (Runtime, LiteWorker, TapeConfig): **None**

The simulation never used these modules. It spawns agents via
`Rho.Agent.Supervisor.start_worker/1` → `Worker` directly.

#### API compatibility: **OK**

| Call site | API | Status |
|-----------|-----|--------|
| simulation.ex:93 | `Rho.Config.agent_config/1` | exists, unchanged |
| simulation.ex:111 | `PluginRegistry.collect_tools/1` | accepts plain map via `Map.get` — OK |
| simulation.ex:115 | `Rho.Stdlib.Tools.Finish.tool_def/0` | exists |
| simulation.ex:119-121 | `Rho.Config.tape_module/0`, `.bootstrap/1` | exist |
| simulation.ex:124 | `Supervisor.start_worker/1` | exists, takes keyword opts |
| simulation.ex:169 | `Worker.whereis/1` | exists |
| simulation.ex:174 | `Worker.submit/3` | exists, `tools:`/`system_prompt:`/`model:` opts handled in legacy path |
| simulation.ex:92 | `Rho.Agent.Primary.{agent_id,new_agent_id}` | exist |

#### Comms usage: **OK, intentionally Comms-only**

Topics: `rho.hiring.scores.submitted` (subscribe), `rho.hiring.simulation.started`,
`rho.hiring.round.started`, `rho.hiring.simulation.completed` (publish).
`Comms.publish/3` and `Comms.subscribe/1` signatures unchanged.

#### Note: legacy worker path

The simulation doesn't pass `:run_spec`, so Worker falls into the legacy
`build_turn_opts_legacy` code path (worker.ex:763). This works today but
should be migrated to RunSpec when the legacy path is eventually removed.

### [ ] Phase 7: Cut over — collapse subscriptions (0.5d)

Remove the old event path.

1. Stop Worker.emit from dual-publishing to Comms (session-scoped events)
2. Remove 3 Comms subscriptions from SessionCore
3. Keep only LiveEvents subscription
4. Remove `signal_for_session?` filtering
5. Remove old string-based dispatch clauses from SessionState

**Notebook:** `notebooks/phase7_cutover.livemd` — run a full agent session
end-to-end on LiveEvents only. Show the single subscription, atom-based
dispatch, and absence of Comms wildcard topics. Before/after subscription
count comparison.

### [ ] Phase 8: FlowLive cleanup (0.5d)

1. FlowLive subscribes via LiveEvents
2. Track `run_id` instead of `generate_agent_id`
3. Remove substring agent_id matching
4. Replace Runtime assign → Scope
5. Use `RhoFrameworks.LLM` for select step loading

**Notebook:** `notebooks/phase8_flow_live.livemd` — demonstrate FlowLive
receiving events via LiveEvents, `run_id` tracking replacing agent_id
substring matching.

### [ ] Phase 9: Plugin cleanup (0.25d)

1. Remove `IdentityPlugin` global registration from `Application.start`
2. Register explicitly per-agent in configs or RunSpec.plugins

**Notebook:** `notebooks/phase9_plugin_cleanup.livemd` — show IdentityPlugin
registered per-agent via RunSpec.plugins instead of global Application.start.
Verify no global side effects on boot.

### Deferred: Hiring simulation

`RhoFrameworks.Demos.Hiring.Simulation` is a multi-agent communication demo
(3 `Comms.publish` calls, `Rho.Agent.*`, `Rho.Config.agent_config`, `Rho.PluginRegistry`).
Kept as-is during this simplification — it will need updating once agent
communication features are ready to showcase. May break if Comms API changes
in Phase 7; pin to pre-refactor API or update opportunistically.

---

## Dependency Graph

```
Phase 0 (rho_baml)     ← foundation for Phases 3 + 4
Phase 1 (LiveEvents)   ← foundation for Phases 2, 5, 6, 7
Phase 2 (dual-path)    ← depends on Phase 1
Phase 3 (BAML LLM)     ← depends on Phase 0
Phase 4 (BAML TypedStructured) ← depends on Phase 3 (follows conventions Phase 3 establishes)
Phase 5 (Scope/AgentJobs)      ← depends on Phase 1
Phase 6 (domain events)        ← depends on Phase 5
Phase 7 (cut over)             ← depends on Phases 2 + 6
Phase 8 (FlowLive)             ← depends on Phases 5 + 7
Phase 9 (plugins)              ← independent cleanup
```

Phases 0+1 can run in parallel. Phase 3 depends on 0; Phase 4 depends on 3
(sequential — Phase 4's SchemaWriter should follow conventions Phase 3 proves out).
Phases 3+4 are independent of Phases 1+2 (different concerns).

---

## Estimated Impact

| Metric | Before | After |
|--------|--------|-------|
| Core deps from frameworks | 7 | 5 (RunSpec/Runner, DataTable, Tool, Effect types, PluginInstance) |
| `Rho.Config.agent_config()` calls | 5 | 0 |
| Manual `Rho.Context` construction | 3 sites | 0 |
| `Rho.Comms.publish` from frameworks | 8 calls (6 files) | 0 |
| Global side effects in Application.start | 1 | 0 |
| Comms subscriptions per SessionLive | 3 wildcards | 1 scoped |
| Structured output parser | Custom Elixir (911 lines: StructuredOutput 537 + SchemaCoerce 374) | BAML Rust NIF |
| Schema source of truth | `.baml` files + separate structs | Zoi schemas (single definition) |
| Model flexibility for structured calls | JSON schema models only | Any model |
| Signal dispatch | String suffix parsing | Atom pattern matching |

**Total effort: ~9.25d** (Phase 5 expanded for LiteWorker removal + MultiAgent migration; SchemaWriter moved from Phase 0 to Phase 4)

---

## Risk Assessment

| Risk | Mitigation |
|------|-----------|
| BAML file I/O on every TypedStructured turn | Profile; `.baml` files are tiny (~500 bytes), local SSD write |
| baml_elixir NIF binary compatibility | Precompiled for darwin-aarch64 + linux; CI verified |
| BAML version churn (pre-release) | Pin exact version; NIF API is stable (call/stream) |
| Collector logs contain API keys | Don't persist Collector logs; use only for debugging |
| Zoi → BAML type mapping gaps | Start with primitives (string, int, float, bool, array, optional); extend as needed |
| Dual LLM calling systems (ReqLLM + BAML) | Clean separation: ReqLLM for agent loop (Direct strategy), BAML for structured output |
| Zoi API changes | Zoi is at 0.18.0 (pre-1.0); pin version, converter is ~30 lines to update |
